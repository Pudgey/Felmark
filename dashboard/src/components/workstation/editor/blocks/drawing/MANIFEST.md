# DrawingBlock -- Manifest

> Auto-maintained by AI. Updated on every file change in this folder.

## Exports
- `DrawingBlock` -- SVG-based drawing block with multiple diagram types (flowchart, mindmap, etc.)
- `DRAWING_TYPE_OPTIONS` -- available drawing type definitions

## Dependencies
- `@/lib/types` -- DrawingBlockData, DrawingType

## Imported By
- `Editor.tsx` -- rendered for drawing block type

## Files
- `DrawingBlock.tsx` -- main component (690 lines)
- `DrawingBlock.module.css` -- styles
