# VisualBlock -- Manifest

> Auto-maintained by AI. Updated on every file change in this folder.

## Exports
- `VisualBlock` -- Meta-block rendering visual variants (process, journey, comparison, before-after, mood, phase)
- `getDefaultVisualData` -- factory for default visual block data by variant

## Dependencies
- `@/lib/types` -- VisualBlockData, VisualVariant, VisualStep, VisualPhase, VisualMoodItem

## Imported By
- `Editor.tsx` -- rendered for visual block type

## Files
- `VisualBlock.tsx` -- main component (221 lines)
- `VisualBlock.module.css` -- styles
