# image/ -- Manifest

> Auto-maintained by AI. Updated on every file change in this folder.

## Exports
- `ImageBlock.tsx` -- single-image block component with upload and URL modes

## Dependencies
- `@/lib/types` -- image block data interface
- `react` -- local input and upload state

## Imported By
- `editor/core/components/block-registry/blockRegistry.ts` -- rendered in the content block map
- `editor/core/components/block-registry/blockDefaults.ts` -- default data factory
- `forge/services/blocks/content.ts` -- forge default data factory

## Files
- `ImageBlock.tsx` -- block component and default data factory
- `ImageBlock.module.css` -- block styles
