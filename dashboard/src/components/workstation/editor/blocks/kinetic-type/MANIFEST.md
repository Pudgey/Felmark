# kinetic-type/ -- Manifest

> Auto-maintained by AI. Updated on every file change in this folder.

## Exports
- `.tsx` (default) -- block component
- `getDefault*` -- default data factory

## Dependencies
- `@/lib/types` -- block data interface
- `../shared/useInView` -- scroll-triggered visibility hook

## Imported By
- `Editor.tsx` -- rendered in contentBlockMap
- `forge/services/blocks/visual.ts` -- default data factory

## Files
- `.tsx.tsx` -- component + default data
- `.tsx.module.css` -- styles
