# EditorMargin -- Manifest

> Auto-maintained by AI. Updated on every file change in this folder.

## Exports
- `EditorMargin` -- Inline fallback margin with outline and block map when the shell-level workstation rail is closed

## Dependencies
- `@/lib/types` -- Block

## Imported By
- `Editor.tsx` -- rendered as inline left document spine only when the shell-level workstation rail is collapsed

## Files
- `EditorMargin.tsx` -- main component (421 lines)
- `EditorMargin.module.css` -- styles
- `rail-data.ts` -- derived workstation-rail summaries, labels, and block-preview helpers shared with the shell-level workstation sidebar
