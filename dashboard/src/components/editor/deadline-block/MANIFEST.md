# DeadlineBlock -- Manifest

> Auto-maintained by AI. Updated on every file change in this folder.

## Exports
- `DeadlineBlock` -- Deadline display with countdown, urgency colors, and date picker
- `getDefaultDeadlineData` -- factory for default deadline data

## Dependencies
- `@/lib/types` -- DeadlineBlockData
- `@/lib/due-dates` -- getDaysLeft, formatDueShort, getDueLabel, getDueColor

## Imported By
- `Editor.tsx` -- rendered for deadline block type

## Files
- `DeadlineBlock.tsx` -- main component (110 lines)
- `DeadlineBlock.module.css` -- styles
