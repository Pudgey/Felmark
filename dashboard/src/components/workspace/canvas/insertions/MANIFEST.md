# Canvas Insertions -- Manifest

> Auto-maintained by AI. Updated on every file change in this folder.

## Exports
- `RowInsertionBar` (default) -- Horizontal "+" between rows
- `ColInsertionBar` (default) -- Vertical "+" between blocks within a row
- `EmptyRow` (default) -- Placeholder for empty rows

## Dependencies
- `./Insertions.module.css`

## Imported By
- `../Canvas.tsx` -- rendered in edit mode between rows/blocks and for empty rows

## Files
- `RowInsertionBar.tsx` -- Horizontal insertion zone with dashed line and + button
- `ColInsertionBar.tsx` -- Vertical insertion zone with dashed line and + button
- `EmptyRow.tsx` -- Empty row placeholder with instructional text
- `Insertions.module.css` -- Insertion zone styles (row, column, empty row)
